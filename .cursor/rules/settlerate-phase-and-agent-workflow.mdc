---
description: SettleRate phase boundaries, agent autonomy, Bugbot triage, and stop conditions
alwaysApply: true
---

# Phase Boundaries

**Phase 1–7A complete on main.** **Phase 7B PAUSED** — live Stripe cutover pending application infrastructure deployment; keep Checkout maintenance on until founder re-authorizes (`docs/PHASE7B_LIVE_STRIPE_CUTOVER_PLAN.md`).

**Phase 8.1** (current, when task is authorized) = production hardening and decoupling per `docs/PHASE8_1_EXECUTION_CHARTER.md` and `docs/PHASE8_1_EPIC_BOUNDARIES.md`.

## Allowed in Phase 8.1 (when the matching epic/PR is authorized)

- Epic work listed as allowed in `docs/PHASE8_1_EPIC_BOUNDARIES.md` for the authorized epic only
- ADRs under `docs/adr/` when required by the authorized epic
- Governance and documentation updates that keep agents aligned with Phase 8.1

## Current epic focus

**Epic 1 — Admin Provisioning Security** is **complete on `main` and fully effective in production**.

**Epic 2 — Environment and Origin Hygiene** is **complete on `main`**
(PR 0–5 merged; ADR 0002). No Supabase Auth Dashboard redirect-allowlist
change was required. Any future local-development redirect additions remain
a separate founder-authorized operational action.

**Epic 3 — Observability** is **complete on `main` and fully effective in
production** (activated and verified 2026-08-06). See
`docs/adr/0003-observability-policy.md`.

**Epic 4 — RLS Security Test Expansion** is **complete on `main`**.
ADR 0004 accepted; PR 0–2 merged; PR 3 not required. See
`docs/adr/0004-rls-testing-standard.md` and
`docs/security/RLS_COVERAGE_INVENTORY.md`.

**Epic 5 — Shared Core Package** is **complete on `main`**. ADR 0005
accepted; PR 0–6 merged. See
`docs/adr/0005-shared-package-architecture.md` and
`docs/EXPORT_CONTRACT.md`.

**Epic 6 — Schema Reconciliation** is **In progress — autonomous
repository completion train** (ADR 0006 and ADR 0007 **accepted**;
PR 0–2C complete/merged). Founder FD-* decisions are **accepted**.
Agents may implement, review, merge, and continue remaining
**repository-side** Epic 6 slices under the Epic 6 Autonomous Completion
Master Prompt. See `docs/adr/0006-database-schema-source-of-truth.md`,
`docs/database/GRANT_REMEDIATION_PR2D.md`, and
`docs/database/EPIC6_PRODUCTION_APPLY_PLAN.md`. **Do not mutate
production without separate founder authorization of the consolidated
apply package.**

## Prohibited unless explicitly authorized

- Supabase Auth Dashboard redirect-allowlist changes without founder authorization
- Changing production Sentry DSNs, project routing, or capture scope without founder authorization
- Beginning Epic 7+ ahead of sequence / dependency gates
- Mutating production databases (DDL/DML/GRANT) / `db push` / `migration repair` without founder authorization
- Resuming Phase 7B live smoke subscription, beta opening, or public checkout
- AWS / Cloudflare migration
- Next.js platform migration
- Calculator formula changes without DEF-*/BM-* gates (Phase 2 authority unchanged)
- Persistence dual-snapshot semantics changes without authorization
- **Export field semantics changes** without explicit architectural approval (see Export fence below)
- Broad entitlement/billing redesign outside the authorized epic
- Weakening CI, RLS, or financial benchmarks

## Export fence (Phase 8.1 hard rule)

Authority: `docs/EXPORT_CONTRACT.md`. Protected surfaces include:

- `docs/EXPORT_CONTRACT.md`
- `supabase/functions/generate-pdf/mapDerivedForExport.ts`
- `src/lib/exports/`

**During Phase 8.1, export field semantics changes are prohibited without explicit architectural approval.**

That means: do not redefine export fields, field meanings, omission rules, snapshot-selection behavior, or other contract behavior.

**Epic 5 clarification:** when Shared Core Package work is authorized, Epic 5 **may** relocate export-related code and update imports. Epic 5 **may not** redefine export fields, meanings, or contract behavior.

**Never begin the next phase or Epic 7+ automatically.** Within the authorized
Epic 6 autonomous repository completion train, continue to the next safe
Epic 6 slice after merge/convergence.

# Agent Autonomy

Unless a prompt says otherwise, agents **may**:

- Inspect the repository
- Create a dedicated branch
- Edit files within authorized phase / epic / PR scope
- Run the full validation sequence
- Create bounded, descriptive commits
- Push the dedicated branch
- Open or update a **draft** PR

**Epic 6 autonomous exception (repository only):** after convergence loops,
agents may mark Ready, merge Epic 6 PRs, delete merged branches, update
main, and continue the next safe Epic 6 slice. Production apply remains
founder-gated via `docs/database/EPIC6_PRODUCTION_APPLY_PLAN.md`.

Agents **may not** without explicit authorization:

- Commit directly to `main` outside Epic 6 merge-via-PR workflow
- Mutate production databases / apply Epic 6 migrations to production
- Begin Epic 7+ or resume Phase 7B live smoke / beta / public checkout
- Disable `CHECKOUT_MAINTENANCE` or open public Checkout while Phase 7B is paused

# Conduct

- Prefer **minimal, localized diffs**; no drive-by refactors
- Match surrounding conventions
- **Report uncertainty**; distinguish confirmed DEF-* defects from proposed improvements
- Do not broaden scope during implementation
- For Epic 6 autonomous train: continue until HARD STOP or Epic 6 closure;
  otherwise stop after the requested task with a completion report

# Bugbot Triage

Bugbot findings are **review inputs, not authority**.

For each finding:

1. Verify against methodology, fixtures, active tests, and code
2. Classify: **accepted**, **rejected**, **partially accepted**, or **requires founder decision**
3. Apply only **accepted** findings in targeted commits (DEF/BM references when financial)
4. **Autofix must remain disabled** for financial-engine work; do not merge autofix branches without triage

**Double-counting principle:** Do not classify financing fees and interest attributable to financed fees as double-counting without checking definitions, timing, and decision horizon in `docs/FINANCIAL_METHODOLOGY.md`.

Document triage decisions in PR description or commit message.
