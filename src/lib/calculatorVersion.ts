/**
 * Semantic version for the SettleRate financial calculators.
 * Increment when calculation logic changes so persisted snapshots remain auditable.
 */
export const CALCULATOR_VERSION = "2.0.0";

/** Schema version for scenario persistence shape (client + derived JSON). */
export const LATEST_SCHEMA_VERSION = 2;
